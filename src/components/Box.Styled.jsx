import styled from "@emotion/styled";
import {
    space,
    layout,
    flexbox,
    grid,
    border,
    shadow,
    position,
} from 'styled-system';

export const Box = styled('div')(
    space,
    layout,
    flexbox,
    grid,
    border,
    shadow,
    position
);